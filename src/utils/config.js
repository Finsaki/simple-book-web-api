//Set the port where you want backend to run
const PORT = "9000";

//For outside connections, include  URI (for example 'http://localhost:3000')
const CONNECTIONURI = "";

//Set if you want database to be populated with mock data on initialization
const USEMOCKDATA = false;

export { PORT, CONNECTIONURI, USEMOCKDATA };
